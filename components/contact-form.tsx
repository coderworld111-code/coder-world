"use client";
import { useState } from "react";
export function ContactForm({quote=false}:{quote?:boolean}){const [status,setStatus]=useState(""); const [busy,setBusy]=useState(false); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setStatus("");const form=e.currentTarget;const data=Object.fromEntries(new FormData(form).entries());const res=await fetch(quote?"/api/quote":"/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const json=await res.json().catch(()=>({}));setBusy(false);if(res.ok){setStatus(quote?"Thanks — your project request has been received.":"Thanks — your message has been received.");form.reset()}else setStatus(json.error?.message||json.error||"Something went wrong. Please try again.")};return <form className="form" onSubmit={submit}><div className="form-grid"><div className="field"><label>Name *</label><input name="name" required/></div><div className="field"><label>Email *</label><input name="email" type="email" required/></div></div><div className="form-grid"><div className="field"><label>Phone</label><input name="phone"/></div><div className="field"><label>Company</label><input name="company"/></div></div>{quote&&<div className="form-grid"><div className="field"><label>Service</label><select name="service"><option>Software Development</option><option>Web Development</option><option>UI/UX Design</option><option>Mobile Development</option><option>AI & Automation</option><option>Business System</option><option>Other</option></select></div><div className="field"><label>Budget</label><select name="budget"><option>To be discussed</option><option>Starter</option><option>Business</option><option>Professional</option><option>Enterprise</option></select></div></div>}<div className="field"><label>Message / project description *</label><textarea name="message" required placeholder="Tell us what you want to build..."/></div>{quote&&<div className="form-grid"><div className="field"><label>Timeline</label><input name="timeline" placeholder="e.g. 6–8 weeks"/></div><div className="field"><label>Project type</label><input name="projectType" placeholder="Website, SaaS, CRM..."/></div></div>}<button className="btn btn-primary" disabled={busy}>{busy?"Sending...":quote?"Submit Project Request →":"Send Message →"}</button>{status&&<div className={status.startsWith("Thanks")?"notice":"toast"}>{status}</div>}</form>}
