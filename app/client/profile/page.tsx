"use client";
import Link from "next/link"; import {useEffect,useState} from "react";
export default function Profile(){const [p,setP]=useState({name:"",phone:"",company:"",email:""});const [msg,setMsg]=useState("");const [busy,setBusy]=useState(false);useEffect(()=>{fetch('/api/profile').then(r=>r.json()).then(j=>{if(j.profile)setP({...p,...j.profile})})},[]);async function save(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg("");const r=await fetch('/api/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(p)});const j=await r.json();setMsg(r.ok?'Profile updated successfully.':j.error?.message||'Unable to update profile.');setBusy(false)}return <main className="admin-shell"><div className="admin-top"><div className="brand"><img src="/coder-world-logo.jpeg" alt="" width="40" height="40"/><span>CODER <span className="gradient">WORLD</span> / PROFILE</span></div><Link className="btn btn-small" href="/client">Dashboard</Link></div><div className="admin-main" style={{maxWidth:760,margin:'0 auto'}}><div className="card"><h1 style={{fontFamily:"'Space Grotesk',sans-serif"}}>Your profile</h1><form className="form" onSubmit={save}><div className="field"><label>Name</label><input value={p.name} onChange={e=>setP({...p,name:e.target.value})} required/></div><div className="field"><label>Email</label><input value={p.email} readOnly/></div><div className="field"><label>Phone</label><input value={p.phone} onChange={e=>setP({...p,phone:e.target.value})}/></div><div className="field"><label>Company</label><input value={p.company} onChange={e=>setP({...p,company:e.target.value})}/></div><button className="btn btn-primary" disabled={busy}>{busy?'Saving...':'Save profile'}</button>{msg&&<div className="notice">{msg}</div>}</form></div></div></main>}
