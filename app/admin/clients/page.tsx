import Link from "next/link"; import { redirect } from "next/navigation"; import { requireAdminSession } from "@/lib/auth"; import { adminDb } from "@/lib/firebase-admin";
export default async function Clients(){const s=await requireAdminSession();if(!s)redirect('/login?error=Unauthorized admin access.');const snap=await adminDb().collection('clients').orderBy('createdAt','desc').get();return <main className="admin-shell"><div className="admin-top"><div className="brand"><img src="/coder-world-logo.jpeg" alt="" width="40" height="40"/><span>CODER <span className="gradient">WORLD</span> / CLIENTS</span></div><Link className="btn btn-small" href="/admin">Dashboard</Link></div><div className="admin-layout"><aside className="admin-side"><Link href="/admin">Dashboard</Link><Link href="/admin/clients">Clients</Link><Link href="/admin/chat">Chat</Link><Link href="/admin/leads">Leads / Quote Requests</Link><Link href="/admin/inquiries">Contact Inbox</Link></aside><section className="admin-main"><span className="eyebrow">Client management</span><h1 style={{fontFamily:"'Space Grotesk',sans-serif",fontSize:48}}>Clients</h1><div className="table-wrap"><table className="table"><thead><tr><th>Name</th><th>Email</th><th>Phone</th><th>Company</th><th>Action</th></tr></thead><tbody>{snap.docs.map(d=>{const c=d.data();return <tr key={d.id}><td>{c.name||"—"}</td><td>{c.email||"—"}</td><td>{c.phone||"—"}</td><td>{c.company||"—"}</td><td><Link className="btn btn-small" href={`/admin/chat/${d.id}`}>Open chat</Link></td></tr>})}</tbody></table>{snap.empty&&<div className="card" style={{margin:16}}>No registered clients yet.</div>}</div></section></div></main>}
